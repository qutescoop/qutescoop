name: Build Windows

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Install Qt
        uses: jurplel/install-qt-action@v2.8.0
        with:
          version: '5.15.1'
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
      - run: dir D:\a\
      - run: dir D:\a\qutescoop/Qt 5.15.1 windows desktop win64_msvc2019_64
      - run: where cl
      - run: qmake QuteScoop.pro
      - run: make 
      - run: make install
      - name: Upload
        uses: actions/upload-artifact@v1
        with:
          name: QuteScoop-Win-${{ github.sha }}.zip
          path: ./DIST-win64
